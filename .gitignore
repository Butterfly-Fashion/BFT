# Vercel & env
.vercel
.env*.local

# Next.js build output
web-b2c/.next/
web-b2b/.next/
web/.next/

# Node modules
node_modules/

# Large static asset folders (not tracked in git)
web-b2c/public/asset/images/
worldcup-figure/

# Playwright test output & auth state
web-b2b/test-results/
web-b2b/tests/*-state.json

# Outreach data files (contact info — not tracked)
scripts/outreach/*.csv
scripts/outreach/*.json
scripts/outreach/*.xlsx
excel/

# Spreadsheets & temp files
*.xlsx
*.xls

# Loose image files in repo root (move to proper folders before committing)
/*.webp
/*.png
/*.jpg
/*.jpeg
sticker-product-img/

# Local editor / agent / CLI state (machine-specific)
.claude/settings.local.json
supabase/.temp/
.playwright-mcp/

# One-off audit reports & scratch folders (kept locally, not tracked)
/*_audit_report_*.md
fb-listing/

# Cold-email outreach tooling (contact info / lists — kept local, not tracked)
cold-email/
