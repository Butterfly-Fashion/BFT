{
  "name": "world-fan-gear-b2b-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:web": "npm install --prefix web",
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "lint": "npm run lint --prefix web",
    "seed": "npm run seed --prefix web",
    "import:products": "npm run import:products --prefix web",
    "start": "npm run start --prefix web"
  }
}
